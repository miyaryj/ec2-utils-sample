require 'spec_helper'

describe Ec2UtilsSample do
  it 'has a version number' do
    expect(Ec2UtilsSample::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
